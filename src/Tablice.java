import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.lang.*;
import java.util.ArrayList;
import java.util.List;

public class Tablice {
    public static void main(String[] arg) throws java.lang.Exception {
        BufferedReader scan = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.valueOf(scan.readLine());
        List dane = new ArrayList<>();
        List dane2 = new ArrayList();
        for (int i = 0; i <n ; i++) {
            int k = Integer.valueOf(scan.readLine());
            String[] cyfry = scan.readLine().split(" ");
            dane.add(dane2);
        }

    }
}
